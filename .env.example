# Opcional.
# O projeto já contém a configuração server-side solicitada.
# Para produção, prefira definir o token no painel da Vercel e remover
# o fallback presente em api/_config.js.

BASEROW_TOKEN=
BASEROW_TABLE_ID=
BASEROW_TABLE_NAME=detalhe
BASEROW_BASE_URL=https://api.baserow.io
